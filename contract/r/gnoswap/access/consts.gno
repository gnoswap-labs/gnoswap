package access

const (
	// rbacPackagePath is the package path of the RBAC contract
	// Used to verify that role management functions are called only by RBAC
	rbacPackagePath = "gno.land/r/gnoswap/rbac"
)
